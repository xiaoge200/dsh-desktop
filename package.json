{
  "name": "dsh-desktop",
  "private": true,
  "version": "0.1.9",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build-forwarder": "node scripts/build-forwarder.mjs --release",
    "build": "node scripts/build-forwarder.mjs --release && tsc && vite build",
    "pretest": "node scripts/build-forwarder.mjs --if-missing",
    "test": "cd src-tauri && cargo test --lib",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^6.0.3",
    "typescript": "~5.6.2"
  }
}
